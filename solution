#include <iostream>
#include <set>

using namespace std;

int main()
{
    string s;
    getline (cin,s);
    set <char> str;
    int i =0;
    while (s[i])
    {
        if(s[i]>='a' && s[i]<='z')
            str.insert(s[i]);
        i++;
    }
    cout<<str.size()<<endl;
    return 0;
}
